# 论文与故事线交接文档

> 用途：将本文档交给一个没有历史上下文的新对话，让它专门帮助梳理研究故事线、论文结构和表述。当前代码主体已经比较稳定，不需要在此对话中重新设计系统或大规模改代码。

## 1. 项目一句话定位

本项目研究一种**面向协作模式的动态多智能体团队系统**：系统不为所有任务固定使用同一组 Agent，而是根据完整任务的实际协作需求选择可复用的 family，并在执行后从 Agent、handoff 和团队模板三个层面沉淀可审计的协作经验。

推荐的核心表述：

> Task-grounded family selection, role-flexible execution, and evidence-driven multi-level team evolution.

中文可表述为：

> 基于任务协作需求选择团队 family，在执行阶段灵活裁剪角色，并通过真实轨迹和历史证据驱动多层团队演化。

## 2. 要解决的问题

固定多智能体系统通常预先指定成员和协作流程，面对不同任务时容易出现角色过多、角色不匹配或验证责任不清。另一方面，仅依据主题、领域或关键词复用团队，会把表面相似误认为协作需求相似，导致 family 过细、模板膨胀或错误复用。

本文真正关注的问题不是“如何增加 Agent 数量”，而是：

1. 如何判断一个新任务应该复用哪一种协作模式；
2. 如何让模板中的候选成员适应当前任务，而不是强制全部执行；
3. 如何将成功的协作方式和失败中的有效证据沉淀为可复用经验；


## 3. 方法主线

### 3.1 Family 选择

Selector 接收完整任务内容和 family 目录。目录中的 family 具有较完整的描述，主要说明：

- 核心协作模式；
- 成员之间的互补关系；
- 证据交接和验证原则；
- 简短的适用边界。

Selector 只有在现有 family 与任务的真实协作需求合理匹配时才复用确切的 `family_id`；不能仅凭主题或词汇相似性复用。没有合理匹配时进入冷启动。

这里的“完整任务”很重要：Selector 不依赖一个被预先抽取、可能丢失关键信息的摘要来做决定。

### 3.2 动态执行

Chairman（当前实现中的 planner）在选定的团队模板中决定本题实际招募哪些 Agent。模板描述的是可复用的协作模式和候选成员，不是每道题必须执行的固定名单。Selector 给出的 reason 只作为软约束，帮助 Chairman 理解任务与 family 的匹配依据，不限制 Chairman 的最终选择。

`chairman` 和 `answer_agent` 不属于团队模板中的可演化协作成员。Answer 负责最终输出，Chairman 负责组织和选择；模板主要记录实际协作角色。

### 3.3 三层演化

任务完成后，系统从三个相互关联但职责不同的层面吸收经验：

- **Agent 层**：更新能力描述、行为提示和可复用 skill；
- **Handoff 层**：基于 Agent 间真实交接轨迹、任务结果和历史规则证据，创建或 refine 协作规则；
- **Team 层**：由 TeamTemplateReflector 判断 family/template 应该 create、reuse、refine 或 retain。

Handoff rule 不是每道题的具体操作记录，而是一般性的交接协议，例如需要交接哪些证据、如何报告验证结果、缺少上下文时如何回退到直接检查。规则属于 `handoff_family_id`，可以在同一 family 下堆叠和版本化；同一条规则被 refine 不等于产生了很多不同规则。

团队 refine 不能只根据当前失败题删除成员。只有历史证据或当前轨迹明确表明成员或规则长期不匹配、功能重复、造成误导/负面影响、反复错误招募或已经被更稳定规则替代时，才考虑移除或替换。

## 4. 与 Meta-Team 的区别

不要把贡献表述为“重新实现 Meta-Team”或“Agent 数量更多”。更准确的区别是：

Meta-Team 的核心贡献是让多智能体系统通过协作反思实现多尺度演化；本项目在此基础上进一步把**协作模式 family**作为显式的选择和记忆单位，强调：

- family 选择由完整任务的实际协作需求驱动，而非主题相似；
- 模板成员是可裁剪的候选角色，执行团队可以小于模板；
- handoff 规则有独立的 family 归属、历史证据和版本化更新；
- TeamTemplateReflector 必须参考成功历史，避免被单个失败样本牵引；
- 结果清单、API 可靠性和版本快照使演化过程可审计、可回退。

因此，故事重点是从“经验驱动的团队更新”推进到“面向协作模式的动态团队组织记忆”。不要声称已经在所有维度上优于 Meta-Team；应通过消融和跨任务结果证明哪些机制有效。

## 5. 实验叙事

实验可以围绕以下问题展开：

1. **Family 选择是否合理？** 是否减少了按主题产生的细碎 family，并能在不同主题下复用相同协作模式；
2. **动态成员选择是否有效？** 模板有多个候选角色时，系统是否只激活当前任务真正需要的成员；
3. **三层演化是否有用？** Agent、handoff、team 三类更新是否分别带来可观察收益；
4. **演化是否稳定？** family/template 是否保持可控规模，是否避免单次失败造成整体漂移；
5. **冻结团队能否迁移？** 训练得到的团队在未见测试任务上是否保持效果。

当前已有的代表性证据：

- LoCoBench FI 训练完成 20 个样本，最终保留 2 个 family、20 个版本快照；训练平均分约 0.617，去掉一个产物未落盘导致的异常 0 分后约 0.650；
- LoCoBench FI 使用最终 v020 演化团队进行 80 题测试，平均分约 0.6593，无 0 分、无基础设施失败和最终 `run_error`；
- LoCoBench CR 的 80 题测试平均分约 0.643，无基础设施失败或最终 `run_error`；
- LOCA 的一次新训练因 MCP 工具初始化不完整而未产生有效结果，这应作为环境/工具可用性问题记录，不应伪装成模型效果结论。

这些数字只作为当前实验记录。论文最终应从 canonical result manifest 和实际 `result.json` 重新汇总，并明确区分模型失败、API 可恢复重试、基础设施失败和产物落盘失败。

## 6. 论文中应避免的表述

- 不要说“family 完全不包含主题信息”；family 描述可以有任务边界，但不能把具体主题当作协作模式本身。
- 不要说“模板中的所有 Agent 都会执行”；模板成员是候选成员，Chairman 可按任务裁剪。
- 不要把 `runner.end.reflection_applied=false` 直接解释为三层反思没有发生；L1/L2、TeamTemplateReflector 和 HandoffReflector 是独立的更新路径。
- 不要把一次 API 错误后的成功重试算作实验失败；必须看最终结果和可靠性记录。
- 不要把 family 数量少直接解释为系统没有动态性；动态性还体现在选择、成员裁剪、技能、handoff 和模板版本演化。
- 不要声称“比 Meta-Team 全面更强”；应说明在哪些设计目标和指标上有改进。

## 7. 新对话的优先任务

新对话开始后，请优先完成以下工作：

1. 把上述方法压缩成论文摘要、Introduction 动机和 Method 总览；
2. 设计一张清晰的系统流程图：完整任务 → family 选择/冷启动 → Chairman 动态选员 → 执行与交接 → 三层反思 → family/template/handoff 持久化；
3. 设计贡献点，避免与 Meta-Team 的贡献重复；
4. 规划消融：无 family 选择、固定团队成员、无 handoff evolution、无历史成功证据约束、无 team-level evolution；
5. 组织主表和分析表，分别回答性能、family 复用、成员裁剪、模板规模和规则演化问题；
6. 检查所有论文数字是否来自最终 manifest/result，而不是日志摘要或旧 run；
7. 对失败案例做定性分析，重点区分协作结构问题、产物验证问题和外部 API/MCP 环境问题。

## 8. 建议的新对话开场指令

你正在帮助撰写一篇关于动态多智能体团队演化的论文。请先基于本文件梳理研究问题、核心假设、方法故事线和与 Meta-Team 的差异，不要先修改代码。重点讨论：为什么 family 应成为协作记忆单位、Selector/Chairman/Reflector 如何分工、三层演化如何形成闭环、哪些实验可以证明这些设计有效，以及哪些结论目前证据不足。
